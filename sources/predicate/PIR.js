/**
 * ==========================
 * @description Predicate intermediate representation (PIR).
 * Used in PADME to get AIRs, which are used it attribute generation algorithms.
 * Is a result of compilation by PADME's compiler.
 * ==========================
 *
 * @author  Evgeny Savelyev
 * @since   19.02.18
 * @version 1.0.0
 * @license See the LICENCE file in the project root.
 */

"use strict";

/* TODO implement */

/**
 * @class
 * @classdesc Predicate intermediate representation (PIR).
 * Used in PADME to get AIRs, which are used it attribute generation algorithms.
 * Is a result of compilation by PADME's compiler.
 */
class PIR {}

exports = module.exports = PIR;